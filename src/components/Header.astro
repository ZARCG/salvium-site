---
import Logo from './Logo.astro';
import { href } from '../lib/href';

// Sticky top nav with a desktop bar (md+) and a mobile drawer (<md).
// Drawer toggle is a tiny inline script — vanilla JS, no React island,
// no hydration cost. The drawer surface uses the .mobile-drawer class
// from global.css so the teal-tinted radial background matches
// siko-ctrl's React drawer.
const path = Astro.url.pathname.replace(/\/$/, '') || '/';
const isActive = (link: string) => {
  const target = href(link).replace(/\/$/, '') || '/';
  if (target === '/' || target === href('/').replace(/\/$/, '')) return path === '/' || path === href('/').replace(/\/$/, '');
  return path === target || path.startsWith(target + '/');
};

const NAV_LINKS = [
  { label: 'About',     href: '/about' },
  { label: 'Download',  href: '/download' },
  { label: 'Pools',     href: '/pools' },
  { label: 'Exchanges', href: '/exchanges' },
  { label: 'Papers',    href: '/papers' },
  { label: 'Blog',      href: '/blog' },
  { label: 'FAQ',       href: '/faq' },
  { label: 'Tools',     href: '/tools' },
  { label: 'Roadmap',   href: '/roadmap' },
  { label: 'Community', href: '/community' },
];
---

<header class="sticky top-0 z-40 border-b border-[color:var(--color-border)] bg-[color:var(--color-bg)]/85 backdrop-blur-sm">
  <nav class="max-w-7xl mx-auto px-5 lg:px-8 h-14 flex items-center justify-between gap-6">
    <a href={href('/')} class="flex items-center hover:opacity-90 transition-opacity" aria-label="Salvium home">
      <Logo className="!h-[22px]" />
    </a>

    <!-- Desktop nav -->
    <div class="hidden md:flex items-center gap-1 text-sm">
      {NAV_LINKS.slice(0, 7).map((link) => (
        <a
          href={href(link.href)}
          class:list={[
            'px-3 py-2 rounded-md transition-colors',
            isActive(link.href)
              ? 'text-white bg-white/[0.04]'
              : 'text-[color:var(--color-text-muted)] hover:text-white hover:bg-white/[0.03]',
          ]}
        >
          {link.label}
        </a>
      ))}
    </div>

    <div class="flex items-center gap-2">
      <a
        href={href('/donate')}
        class="btn-primary hidden sm:inline-flex !text-[0.72rem] !py-[0.56rem] !px-[1.12rem]"
      >
        Donate
      </a>

      <!-- Mobile menu button — visible <md only -->
      <button
        type="button"
        id="mobile-menu-toggle"
        aria-expanded="false"
        aria-controls="mobile-drawer"
        aria-label="Open menu"
        class="md:hidden inline-flex items-center justify-center w-10 h-10 rounded-md border border-white/10 bg-white/[0.02] text-white hover:bg-white/[0.06] transition-colors"
      >
        <svg id="menu-icon-open" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="18" x2="21" y2="18"/>
        </svg>
        <svg id="menu-icon-close" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" class="hidden">
          <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>
  </nav>

  <!-- Mobile drawer. Hidden by default; the inline script below toggles
       data-open which triggers the CSS transition. Inline styles for
       position/background so we don't depend on any utility class
       generating during build (the previous build had the drawer ending
       at content height with the page bleeding through). 100dvh handles
       mobile browsers' dynamic viewport / URL-bar collapse. -->
  <div
    id="mobile-drawer"
    class="md:hidden"
    role="dialog"
    aria-modal="true"
    aria-label="Site menu"
  >
    <nav class="drawer-nav">
      <ul>
        {NAV_LINKS.map((link) => (
          <li>
            <a
              href={href(link.href)}
              class:list={[
                'drawer-link',
                isActive(link.href) ? 'is-active' : '',
              ]}
            >
              {link.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
    <div class="drawer-cta">
      <a href={href('/donate')} class="btn-primary w-full justify-center">
        Donate
      </a>
    </div>
  </div>
</header>

<style>
  /* Drawer positioning + background — inline to avoid any Tailwind v4
     class-generation timing issues. data-open is added by the toggle
     script. */
  #mobile-drawer {
    position: fixed;
    inset: 0;
    z-index: 50;
    display: flex;
    flex-direction: column;
    /* dvh handles mobile browsers shrinking the viewport when the URL
       bar collapses; vh fallback for older browsers. */
    height: 100vh;
    height: 100dvh;
    padding-top: 3.5rem;
    background:
      radial-gradient(900px 600px at 10% -10%, rgba(0, 191, 165, 0.18), transparent 60%),
      radial-gradient(700px 500px at 110% 110%, rgba(10, 235, 133, 0.10), transparent 55%),
      rgba(13, 13, 13, 0.98);
    opacity: 0;
    visibility: hidden;
    transform: translateY(8px);
    transition: opacity 200ms ease, transform 200ms ease, visibility 0s linear 200ms;
    pointer-events: none;
  }
  #mobile-drawer[data-open="true"] {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
    transition: opacity 200ms ease, transform 200ms ease, visibility 0s linear 0s;
    pointer-events: auto;
  }
  #mobile-drawer .drawer-nav {
    flex: 1 1 auto;
    overflow-y: auto;
    padding: 1.5rem 1.25rem;
  }
  #mobile-drawer .drawer-nav ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }
  #mobile-drawer .drawer-link {
    display: block;
    padding: 0.75rem 1rem;
    border-radius: 0.5rem;
    font-size: 1rem;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.8);
    border: 1px solid transparent;
    transition: color 150ms ease, background 150ms ease, border-color 150ms ease;
  }
  #mobile-drawer .drawer-link:hover {
    color: #fff;
    background: rgba(255, 255, 255, 0.04);
  }
  #mobile-drawer .drawer-link.is-active {
    color: #fff;
    background: rgba(255, 255, 255, 0.06);
    border-color: rgba(255, 255, 255, 0.10);
  }
  #mobile-drawer .drawer-cta {
    flex: 0 0 auto;
    padding: 0 1.25rem 1.5rem;
    padding-bottom: calc(1.5rem + env(safe-area-inset-bottom));
  }
</style>

<script is:inline>
  (function () {
    const toggle = document.getElementById('mobile-menu-toggle');
    const drawer = document.getElementById('mobile-drawer');
    const iconOpen = document.getElementById('menu-icon-open');
    const iconClose = document.getElementById('menu-icon-close');
    if (!toggle || !drawer) return;

    function setOpen(open) {
      toggle.setAttribute('aria-expanded', String(open));
      toggle.setAttribute('aria-label', open ? 'Close menu' : 'Open menu');
      // The drawer's open/closed state is driven by data-open in CSS so
      // we never have to fight Tailwind utility class generation.
      drawer.dataset.open = open ? 'true' : 'false';
      iconOpen?.classList.toggle('hidden', open);
      iconClose?.classList.toggle('hidden', !open);
      document.body.style.overflow = open ? 'hidden' : '';
    }

    toggle.addEventListener('click', () => {
      const isOpen = toggle.getAttribute('aria-expanded') === 'true';
      setOpen(!isOpen);
    });

    drawer.querySelectorAll('a').forEach((a) => a.addEventListener('click', () => setOpen(false)));
    document.addEventListener('keydown', (e) => { if (e.key === 'Escape') setOpen(false); });
    window.matchMedia('(min-width: 768px)').addEventListener('change', (e) => { if (e.matches) setOpen(false); });
  })();
</script>
